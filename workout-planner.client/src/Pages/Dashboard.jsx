
import React from 'react';

const Dashboard = () => {
    return (
        <div>
            <h1>Dashboard</h1>
            <p>This content is now perfectly aligned inside the layout!</p>
        </div>
    );
};

export default Dashboard;